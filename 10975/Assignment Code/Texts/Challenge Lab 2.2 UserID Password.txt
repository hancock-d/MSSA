2. Write a C# Sharp program that takes userid and password as input (type string). After 3 wrong attempts, user will be rejected.

User inputs userid and password (simple string values)
Check whether they're the correct combination or not, have to compare it with something
hard-coded values
	userid = Admin
	password = Password
	max attempts = 3
If login successful; login successful
If unsuccessful; provide another attempt
	increment counter to 2
provide another attempt
	increment counter to 3
		user locked
Can use while, can use for, can use decision making statements, whatever you prefer